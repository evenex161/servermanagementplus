[versions]
# Minecraft
minecraft = "1.21.4"
parchment-minecraft = "1.21.4"
parchment = "2024.11.10"
neoform = "1.21.4-20241215.123456"

# Loaders & Platforms
forge = "54.1.0"
neoforge = "21.4.15"
fabric-api = "0.116.1+1.21.4"
fabric-loader = "0.18.1"

# Build Plugins (declared in build.gradle, referenced here for single-source-of-truth)
fabric-loom = "1.9.2"
moddev = "2.0.49-beta"

# Common Libraries
mixin = "0.8.5"
mixinextras = "0.3.5"
jopt-simple = "5.0.4"

[libraries]
# Forge
forge = { module = "net.minecraftforge:forge", version.ref = "forge" }

# NeoForge (version set via neoForge.version in build.gradle)
# neoforge — managed by moddev plugin, not a direct dependency

# Fabric
fabric-api = { module = "net.fabricmc.fabric-api:fabric-api", version.ref = "fabric-api" }
fabric-loader = { module = "net.fabricmc:fabric-loader", version.ref = "fabric-loader" }

# Common
minecraft = { module = "com.mojang:minecraft", version.ref = "minecraft" }
mixin = { module = "org.spongepowered:mixin", version.ref = "mixin" }
mixinextras = { module = "io.github.llamalad7:mixinextras-common", version.ref = "mixinextras" }
jopt-simple = { module = "net.sf.jopt-simple:jopt-simple", version.ref = "jopt-simple" }

[plugins]
fabric-loom = { id = "fabric-loom", version.ref = "fabric-loom" }
moddev = { id = "net.neoforged.moddev", version.ref = "moddev" }
